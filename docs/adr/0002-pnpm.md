# ADR 0002: pnpm

Status: accepted.

Use the pinned pnpm version and one `pnpm-lock.yaml`. pnpm provides deterministic CI installs, strict dependency resolution and compatible Cloudflare/Vinext workflows. Do not commit npm or yarn lockfiles.
